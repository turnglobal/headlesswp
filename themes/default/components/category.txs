{{#if categories.length}}
  <div class="flex flex-wrap gap-2">
    {{#each categories}}
      <span class="text-[11px] px-2.5 py-1 rounded-full surface-muted text-muted border border-gray-200 dark:border-zinc-700">{{name}}</span>
    {{/each}}
  </div>
{{/if}}
